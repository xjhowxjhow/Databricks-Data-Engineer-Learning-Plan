# The main pipeline for lakeflow_dividas_ativas_dtl

resources:
  pipelines:
    lakeflow_dividas_ativas_dtl_etl:
      name: lakeflow_dividas_ativas_dtl_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/lakeflow_dividas_ativas_dtl_etl"

      libraries:
        - glob:
            include: ../src/lakeflow_dividas_ativas_dtl_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
